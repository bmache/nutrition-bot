# CrewAI 1.x uses native provider SDKs. The google-genai extra is
# included because gemini is the default provider in .env.example.
# Other providers need their own extra:
#   ollama      -> no extra needed
#   openrouter  -> uv pip install "crewai[openai]"
#   watsonx     -> uv pip install "crewai[watsonx]"
crewai[google-genai]>=1.0.0
litellm>=1.55.0
gradio>=6.0.0
pydantic>=2.7.0
python-dotenv>=1.0.0
pillow>=10.0.0
requests>=2.31.0
PyYAML>=6.0
pytest>=8.0.0
